# todo_backend
demonstrate sql database link to python flask backend, showing todo_app entries and storing usernames + password in a hashed format + encryption

run web_server/run.py

start the virtual environment via ".\web_server\env\Scripts\activate"
